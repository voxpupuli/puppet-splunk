---
version: 5

defaults:
  datadir: data
  data_hash: yaml_data

hierarchy:
  - name: "OS family"
    path: "os/%{facts.os.family}.yaml"

  - name: "Kernel"
    path: "kernel/%{facts.kernel}.yaml"

  - name: "common"
    path: "common.yaml"
